# 

Install pytest, pytest-cov, and pyright as dev dependencies.
    Learn the difference between dev dependencies and prod.


Be sure ruff plugin is installed in vscode automatically,
    be sure it is configured to run formatting automatically.
    May need to provide some configuration heads up here?
        It's not trivial.

Setup CI action. Make a PR. See that the PR attempts to run
   on me